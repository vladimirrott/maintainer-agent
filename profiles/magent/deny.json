{
 "_comment": [
  "Inputs to the deny wall. scripts/render-settings.py expands this into",
  "settings.json (live) and settings-rehearsal.json (live + every GitHub",
  "write verb). Every verb here is spelled from every directory it could",
  "be run from, including the ones the tools are actually installed in:",
  "a rule for `cargo publish` that misses ~/.cargo/bin protects nothing.",
  "The bound is honest: enumeration cannot be complete, so this stops a",
  "cooperative agent reaching a destructive verb, not a hostile one.",
  "The receipt-signing key sits in credential_paths on purpose: a receipt",
  "is only unforgeable while the key is unreadable, so it belongs beside",
  "the SSH keys rather than in a list of its own."
 ],
 "spelled_everywhere": [
  "cargo publish",
  "curl",
  "gh api --method DELETE",
  "gh api --method PUT",
  "gh api -X DELETE",
  "gh api -X PUT",
  "gh auth token",
  "gh pr create",
  "gh pr merge",
  "gh release",
  "gh repo delete",
  "gh repo edit",
  "gh secret",
  "gh variable",
  "gh workflow run",
  "git -C",
  "git checkout --",
  "git cherry-pick",
  "git clean",
  "git commit",
  "git merge",
  "git push",
  "git rebase",
  "git reset --hard",
  "git restore",
  "git tag",
  "maintainer-merge receipt",
  "nc",
  "npm publish",
  "printenv",
  "wget"
 ],
 "bare_exact": [
  "env"
 ],
 "credential_paths": [
  "~/.ssh/**",
  "~/.aws/**",
  "~/.gnupg/**",
  "~/.netrc",
  "~/.config/gh/**",
  "~/.claude/.credentials.json",
  "~/.local/share/maintainer/receipt.key"
 ]
}